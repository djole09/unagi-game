import React from 'react';

import { LoadingContainer, LoadingStyled } from './Loading.style';

export const Loading = () => (
  <LoadingContainer>
    <h2>Loading</h2>
    <LoadingStyled />
  </LoadingContainer>
);
